<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <solid android:color="@color/light_purple"/>
    <corners android:radius="100dp"/>
    <!-- Optional border -->
    <stroke
        android:width="2dp"
        android:color="#FFFFFF" />

    <!-- Size (important for perfect circle) -->
    <size
        android:width="40dp"
        android:height="40dp" />
</shape>
